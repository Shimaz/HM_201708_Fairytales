<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.tangomike.hm_201708_fairytales">

    <uses-sdk android:minSdkVersion="23" android:maxSdkVersion="26" android:targetSdkVersion="26"></uses-sdk>

    <application
        android:name=".DataCollection"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <activity android:name=".MainActivity"
            android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IndexActivity"
            android:screenOrientation="sensorPortrait"/>
        <activity android:name=".PageActivity"
            android:screenOrientation="sensorPortrait"></activity>
        <activity android:name=".InfoActivity"
            android:screenOrientation="sensorPortrait"/>
    </application>

</manifest>